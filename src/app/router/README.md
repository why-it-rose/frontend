# app/router

앱 전체 라우팅 구성을 정의하는 폴더.
react-router v7 기반의 라우트 트리와 경로 상수를 관리한다.

## 예시 파일
- `index.tsx` — `createBrowserRouter`로 라우트 트리 정의
- `paths.ts` — 경로 상수 (`/`, `/stocks/:code`, `/alerts`, `/mypage`)
