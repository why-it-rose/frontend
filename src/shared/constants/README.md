# shared/constants

앱 전체에서 공통으로 참조하는 상수를 두는 폴더.

## 예시 파일
- `routes.ts` — 라우트 경로 상수 (`ROUTES.HOME`, `ROUTES.STOCK_DETAIL` 등)
- `queryKeys.ts` — TanStack Query 쿼리 키 상수 (features/의 queryKeys와 중복 방지를 위해 shared에 통합 관리할 경우)
