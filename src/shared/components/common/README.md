# shared/components/common

여러 페이지·피처에서 재사용되는 범용 UI 컴포넌트를 두는 폴더.
비즈니스 로직을 포함하지 않으며 props로만 동작하는 순수 UI 컴포넌트를 관리한다.

## 예시 파일
- `Button.tsx` — 범용 버튼 (variant, size, disabled 등 props 지원)
- `Badge.tsx` — 태그·상태 표시용 뱃지 (색상 variant 지원)
