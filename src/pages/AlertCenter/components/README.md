# pages/AlertCenter/components

알림센터 페이지 전용 레이아웃·조합 컴포넌트를 두는 폴더.
이 폴더의 컴포넌트는 알림센터 페이지 외 다른 곳에서 사용하지 않는다.

## 예시 파일
- `AlertCenterLayout.tsx` — 필터 바 + 뉴스 목록 전체 레이아웃
- `AlertFilterBar.tsx` — 종목·태그·날짜·읽음 상태 필터 조합 UI
